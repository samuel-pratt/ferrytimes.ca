#page title
page_title: # Automatically populates with app name if not set and if iOS app ID is set. Otherwise enter manually.

# App Info
ios_app_id: 1615899209 # Required. Enter iOS app ID to automatically populate name, price and icons (e.g. 718043190).

appstore_link: # Automatically populates if not set and if iOS app ID is set. Otherwise enter manually.
playstore_link: # Enter Google Play Store URL.
presskit_download_link:# Enter a link to downloadable file or (e.g. public Dropbox link to a .zip file).
  # Or upload your press kit file to assets and set path accordingly (e.g. "assets/your_press_kit.zip").

app_icon: # Automatically populates if not set and if iOS app ID is set.  Otherwise enter path to icon file manually.
app_name: # Automatically populates if not set and if iOS app ID is set.  Otherwise enter manually.
app_price: # Automatically populates if not set and if iOS app ID is set.  Otherwise enter manually.
app_description: Get real time sailing and capacity info for your next BC Ferries voyage with BC Ferry Times.

enable_smart_app_banner: true # Set to true to show a smart app banner at top of page on mobile devices.

# Information About Yourself
your_name: Sam Pratt
your_link: https://sampratt.dev
your_city: Victoria, BC
email_address:
facebook_username:
instagram_username:
twitter_username:
github_username:
youtube_username:

# Feature List                            Edit, add or remove features to be presented.
features:
  - title: Sailing times
    description: Get up-to-date ferry schedules at every major terminal.
    fontawesome_icon_name: clock

  - title: Capacity information
    description: Accurate capacity information for each route.
    fontawesome_icon_name: car

  - title: Sailing information
    description: Details like vessel name, sailing suration, and a fill breakdown.
    fontawesome_icon_name: info

# Theme Settings
topbar_color: "#000000"
topbar_transparency: 0
topbar_title_color: "#fff"

cover_image: #assets/headerimage.png                    # Replace with alternative image path or image URL.
cover_overlay_color: "#000000"
cover_overlay_transparency: 0

device_color: black # Set to: blue, black, yellow, coral or white.

body_background_color: "#1E1E1E"

link_color: "#4592CA"

app_title_color: "#fff"
app_price_color: "#fff"
app_description_color: "#fff"

feature_title_color: "#ffffff"
feature_text_color: "#666666"

feature_icons_foreground_color: "#4592CA"
feature_icons_background_color: "#fff"

social_icons_foreground_color: "#4592CA"
social_icons_background_color: "#fff"

footer_text_color: "#fff"

####################################################
### Jekyll Configuration. No need to touch this. ###
####################################################

# Set the Sass partials directory, as we're using @imports
sass:
  style: :compressed # You might prefer to minify using :compressed

# Exclude these files from your production _site
exclude:
  - LICENSE
  - README.md
  - CNAME

collections:
  pages:
    output: true
    permalink: /:path/

# Markdown rendering
markdown: kramdown
